class MyString {
  constructor(param) {
    this.string = param;
    this.myLength = 0;
  }

  countLength() {
    let count = 0;

    for (let i = 0; i < this.string.length; i++) {
      let;
    }
  }

  greeting() {
    console.log(`Hello, ${this.string}`);
  }
}
let _str = 'Shakib';
const str = new MyString(_str);

str.greeting();
